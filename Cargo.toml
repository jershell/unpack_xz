[package]
name = "unpack_xz"
version = "0.0.1"
edition = "2021"

[dependencies]
lzma-rs = "0.3.0"
wasm-bindgen = "0.2"


[lib]
crate-type = ["cdylib"]

[profile.release]
lto = true
opt-level = "z"  # Максимальная оптимизация размера
codegen-units = 1
strip = true  # убирает дебажные символы